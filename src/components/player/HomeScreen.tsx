// HomeScreen component — reusable player home wrapper
// The actual implementation is in /app/home/page.tsx (server component)
// This component is reserved for client-side interactive bits of the home screen

export {}
